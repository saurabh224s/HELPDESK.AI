# Security

Do not publish credentials, API keys, private user data, or active exploit details in public issues.

Keep secrets in environment variables or a supported secret manager. If a credential is exposed, rotate it immediately.

For vulnerability reports, provide the affected component/version, reproducible steps, expected versus observed behavior, and logs with secrets removed. Use a private reporting channel when the repository provides one.
